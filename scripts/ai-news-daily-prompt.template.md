你现在是在可执行的定时任务上下文，不需要用户确认。
请立即通过 `exec` 在本机执行下列命令，并原样返回命令输出：

`bash /Users/dysania/program/openclaw/scripts/run-ai-news-daily-pipeline.sh`

执行要求：
1. 不要把这条消息当作“提醒转述”，不要回复“我可以执行/请确认”。
2. 不要改写命令，不要拆步骤，不要输出计划。
3. 若命令失败，返回失败的原始 stderr 与退出原因。
